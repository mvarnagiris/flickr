<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <com.code44.flickr.views.PhotoView
        android:id="@+id/leftPhoto_V"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- I know nested weights are bad, it's just quicker this way than extending ViewGroup-->

        <com.code44.flickr.views.PhotoView
            android:id="@+id/topRightPhoto_V"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <com.code44.flickr.views.PhotoView
            android:id="@+id/bottomRightPhoto_V"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>

</merge>